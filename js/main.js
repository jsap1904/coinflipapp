
function coinToss {
	var chance = Math.floor(Math.random() * 2)
	var coinFlip = document.getElementById("coinFlipper").value

	if (chance ==0) {
		coinFlipper.src = img 1

	} else {
		coinFlipper.src = img 2
	}
}