// var heads = 1
// var tails = 0
// var userPick = userInput.value
// var comPick = (Math.random() * 2)


// function coinToss {
// 	console.log(Math.random() * 2)
// if (userPick &&== heads)
// 	console.log("")
// 		

function coinToss {
	var chance = Math.floor(Math.random() * 2)
	var coinFlip = document.getElementById("coinFlipper")

	if (chance ==0) {
		coinFlipper.src = img 1

	} else {
		coinFlipper.src = img 2
	}
}